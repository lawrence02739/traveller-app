import type { DashboardLayoutProps } from './types';
export declare const DashboardLayout: ({ portalTitle, navItems, activeLabel, userName, userRole, pageTitle, pageSubtitle, headerActions, children, }: DashboardLayoutProps) => JSX.Element;
